# third partied
.rvm
.gem

# don't track this!
.ssh

# histories
.*-history
.*_history
.lesshst

# caches
.viminfo
.zcompdump

# now, ignore things that are real files, 
# because they'll probably never be part of the repo
#
[A-Za-z]*
